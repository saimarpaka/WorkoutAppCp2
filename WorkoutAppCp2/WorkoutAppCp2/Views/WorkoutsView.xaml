<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:WorkoutAppCp2.Controls"
             x:Class="WorkoutAppCp2.Views.WorkoutsView"
             x:Name="RootPage">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <controls:RepeaterView ItemsSource="{Binding NamesList}" Spacing="10" Padding="6">
                    <controls:RepeaterView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <controls:CardView HasShadow="True">
                                <StackLayout>
                                    
                                        <controls:RepeaterView ItemsSource="{Binding days}" Spacing="10" Padding="6">
                                            <controls:RepeaterView.ItemTemplate>
                                                <DataTemplate>
                                                    <ViewCell>
                                                        <StackLayout>
                                                            <controls:CardView BackgroundColor="#f2f4f5" HasShadow="True">
                                                                <Label Text="{Binding Day}"></Label>
                                                            </controls:CardView>
                                                        </StackLayout>

                                                    </ViewCell>
                                                </DataTemplate>
                                            </controls:RepeaterView.ItemTemplate>
                                        </controls:RepeaterView>


                                        <Button Text="Add Day" Command="{Binding Path=BindingContext.AddDayCommand, Source={x:Reference RootPage}}" CommandParameter="{Binding .}" ></Button>
                                </StackLayout>
                                </controls:CardView>
                            </ViewCell>
                        </DataTemplate>
                        
                    </controls:RepeaterView.ItemTemplate>
                    
                </controls:RepeaterView>
                <Button Text=" Click me" Command="{Binding AddCommand}" ></Button>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>